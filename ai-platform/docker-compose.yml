services:
  proxy-gateway:
    build: ./proxy-gateway
    image: ai-proxy-gateway
    container_name: ai-gateway
    cap_add:
      - NET_ADMIN
      - NET_RAW
    ports:
      - "9901:6080"
    environment:
      - PROXY_HOST=${PROXY_HOST}
      - PROXY_PORT=${PROXY_PORT}
      - PROXY_USER=${PROXY_USER}
      - PROXY_PASS=${PROXY_PASS}
    networks:
      - proxy-net
    healthcheck:
      # kill switch rules present AND redsocks listening on 12345 (0x3039)
      test: ["CMD-SHELL", "iptables -t nat -S OUTPUT | grep -q REDSOCKS && grep -q ':3039' /proc/net/tcp"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    restart: unless-stopped

  claude-container:
    build: ./claude-container
    image: ai-claude
    container_name: ai-claude
    network_mode: "service:proxy-gateway"
    depends_on:
      proxy-gateway:
        condition: service_healthy
    environment:
      - HTTP_PROXY=http://${PROXY_USER}:${PROXY_PASS}@${PROXY_HOST}:${PROXY_PORT}
      - HTTPS_PROXY=http://${PROXY_USER}:${PROXY_PASS}@${PROXY_HOST}:${PROXY_PORT}
      - NO_PROXY=localhost,127.0.0.1
    volumes:
      - ${PROJECTS_DIR}:/workspace
      - claude-auth:/home/claude/.claude
    stdin_open: true
    tty: true
    restart: unless-stopped

  browser-container:
    build: ./browser-container
    image: ai-browser
    container_name: ai-browser
    network_mode: "service:proxy-gateway"
    depends_on:
      proxy-gateway:
        condition: service_healthy
    environment:
      - PROXY_HOST=${PROXY_HOST}
      - PROXY_PORT=${PROXY_PORT}
      - VNC_PASSWORD=${VNC_PASSWORD:-claude}
    volumes:
      - browser-profile:/home/claude/.mozilla
      - browser-config:/home/claude/.config
    stdin_open: true
    tty: true
    restart: unless-stopped

  backend:
    build: ./backend
    image: ai-backend
    container_name: ai-backend
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${PROJECTS_DIR}:/workspace
    environment:
      - PORT=3000
      - CLAUDE_CONTAINER=ai-claude
      - BROWSER_CONTAINER=ai-browser
    depends_on:
      claude-container:
        condition: service_started
      browser-container:
        condition: service_started
    networks:
      - app-net
    restart: unless-stopped

  frontend:
    build: ./frontend
    image: ai-frontend
    container_name: ai-frontend
    environment:
      - UI_USER=${UI_USER:-admin}
      - UI_PASSWORD=${UI_PASSWORD:-}
    ports:
      - "9900:80"
    depends_on:
      - backend
    networks:
      - app-net
    restart: unless-stopped

volumes:
  claude-auth:
    driver: local
  browser-profile:
    driver: local
  browser-config:
    driver: local

networks:
  proxy-net:
    driver: bridge
  app-net:
    driver: bridge
