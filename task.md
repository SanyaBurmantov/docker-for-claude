Мне нужно создать полностью изолированное Docker-окружение для работы с Claude Code CLI через прокси.

Мои условия:

Я нахожусь в Беларуси.
Мне нужно стабильное окружение для работы с Claude.
Я хочу запускать Claude в терминале как обычно (claude), но весь его сетевой трафик должен идти только через мой прокси.
Нужно исключить прямой выход в интернет из контейнера.
Авторизация Claude происходит через браузер, поэтому браузер тоже должен быть внутри контейнера.
Нужно сделать полноценное рабочее окружение, а не просто контейнер с переменной HTTP_PROXY.

Мой прокси:

Тип: IPv4 HTTPS/HTTP proxy
Host: 88.218.50.200
Port: 8000
Username: k0J9gL
Password: 8LyqEt

Нужно сделать:

1. Docker-архитектуру

Предложи и реализуй Docker-систему примерно такого вида:

Host machine
|
|
Docker container
|
├── Ubuntu
├── Node.js
├── Claude Code CLI
├── Browser (Firefox/Chromium)
├── Desktop environment (XFCE или аналог)
├── noVNC для доступа к браузеру через localhost
├── Proxy client (если нужен)
└── Network isolation
|
|
HTTP/SOCKS Proxy
|
|
Internet

Цель:

открыть браузер на localhost:6080;
увидеть рабочий Linux Desktop внутри контейнера;
открыть терминал;
выполнить claude;
пройти авторизацию Claude через браузер внутри контейнера;
после этого работать как обычно.
2. Требования к безопасности сети

Очень важно:

Простого:

HTTP_PROXY=http://...
HTTPS_PROXY=http://...

может быть недостаточно.

Нужно предусмотреть вариант, где:

если приложение игнорирует HTTP_PROXY, трафик всё равно идет через прокси;
контейнер не может выйти напрямую в интернет;
при падении прокси интернет внутри контейнера должен перестать работать (kill switch).

Рассмотри варианты:

sing-box;
tun2socks;
redsocks;
iptables;
отдельный proxy gateway контейнер.

Выбери наиболее надежный вариант и объясни почему.

3. Авторизация Claude

Учти, что Claude Code требует OAuth авторизацию через браузер.

Нужно:

браузер должен находиться внутри контейнера;
браузер должен использовать тот же прокси;
cookies/token должны сохраняться между перезапусками контейнера;
не нужно каждый раз логиниться заново.
4. Сохранение данных

Нужно сделать volume:

чтобы сохранялась авторизация Claude;
чтобы сохранялись настройки браузера;
чтобы рабочие проекты можно было монтировать с хоста.

Например:

~/projects
|
|
└── /workspace внутри контейнера
5. Интерфейс использования

Я хочу максимально простой workflow:

Первый запуск:

docker compose build
docker compose up -d

Открываю:

http://localhost:6080

Логинюсь в Claude.

Дальше каждый день:

docker exec -it claude-env bash

claude

или желательно сделать короткую команду типа:

claude-box

которая сама входит в контейнер.

6. Что нужно предоставить в ответе

Нужен полный готовый проект:

Структура:

claude-docker/
├── Dockerfile
├── docker-compose.yml
├── .env.example
├── start.sh
├── network setup scripts
└── README.md

Нужно написать:

полный Dockerfile;
полный docker-compose.yml;
все shell-скрипты;
инструкции запуска;
команды проверки;
команды диагностики, если прокси не работает.
7. Проверки после запуска

Нужно добавить проверки:

Проверка внешнего IP:
curl https://api.ipify.org

Должен показывать IP прокси:

88.218.50.200

а не реальный IP пользователя.

Проверка, что прямой интернет заблокирован.
Проверка, что Claude работает:
claude
8. Дополнительные пожелания

Предпочтительно:

Ubuntu 24.04;
Node.js LTS;
Claude Code последней версии;
Firefox или Chromium;
сохранение состояния через Docker volumes;
возможность работать с проектами через mounted volumes;
минимальное количество ручных действий.

Не предлагай только простое использование VPN или смену IP. Нужен именно Docker-контейнер с браузером и принудительным проксированием всего трафика.

Это должно получиться как отдельная изолированная рабочая станция Claude Code, где весь сетевой доступ контролируется прокси.