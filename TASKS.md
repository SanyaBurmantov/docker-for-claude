# Tasks

- [x] Добавить кнопку "Создать commit message" на вкладке git
- [x] Добавить текущее время в хедер.
- [x] Установить opencode и добавить возможность использовать не только claude, а еще и другие модели (Big Piccle)
- [x] Проекты сортировать по дате последнего открытия их в этой платформе, либо по дате последнего изменения. Добавить "Избранные проекты", чтобы они грубо говоря были вверху в отдельной рамочке (для удобства)
- [x] ~~По возможности вынести загруженность контекста отдельно, а так же время обновления его~~ — отменено, панель удалена
- [ ] в tasks "Обсудить" надо делать в модальном окошке
- [x] буквально обсудить разработка loop-менеджера для выполнения задач, придумать реализацию роли аналитик и менеджер — спроектировано, см. `ai-platform/docs/loop-manager-spec.md`
- [x] Реализовать loop-менеджер по спецификации `ai-platform/docs/loop-manager-spec.md` (промт: `ai-platform/docs/loop-manager-prompt.md`). Роли: менеджер, аналитик, исполнитель, тестировщик, код-ревьюер. Делегирование по сложности (haiku/sonnet/opus + opencode/deepseek + gemini-flash), гейт перед правками, escalation ladder, панель менеджера справа (клон Gemini) — все три среза (Claude-каркас, opencode/gemini движки, автостарт из TASKS.md + эксклюзивный лок) сделаны на ветке `feat/loop-manager`. Не проверено вживую на реальном контейнере (в песочнице разработки нет Docker); JSON-парсинг вывода opencode — эвристика, требует сверки с реальной транскрипцией при первом прогоне.
- [x] Первый живой прогон loop-менеджера показал: реплики человека из композера пропадали без следа (уходили только в `humanNotes` для промпта менеджера, нигде не отображались) — панель теперь показывает их как отдельные карточки в ленте (SSE-событие `note`); долгий connected implement→verify retry без прогресса выглядел как «зависание» — добавлена подсказка попытки/tier/причины прямо над лентой. Вторым проходом найдены и исправлены логические ошибки FSM: бесконечный цикл done→review (кейс `review` не обновлял `verifiedDiffSha` — теперь чистое ревью догоняет тестами и верифицирует diff), `[policy]`-спам в `humanNotes`, потеря Stop вне LLM-вызова (набор `stopRequests`), `reset --hard` при эскалации стирал незакоммиченные правки (чекпоинт взводится только на чистом дереве), дедлайн съедался ожиданием на гейте (`budgetResumedAt`), излишне строгая валидация `executor` в decision-JSON. JSON-парсинг вывода opencode (`extractOpencodeText`) по-прежнему не сверен с реальной транскрипцией.
- [x] Адаптировать наработки loop-менеджера для экономии токенов в интерактивных сессиях Claude Code: добавлен `ai-platform/scripts/delegate.mjs` — вызов дешёвого движка (opencode/deepseek с инструментами в контейнере, gemini текст-в/текст-из через бэкенд) из Bash; задокументировано в `ai-platform/CLAUDE.md` («Делегирование дешёвым моделям»). Архитектура перевёрнута: менеджером выступает сам Claude Code, дешёвые модели — исполнители механической работы. Путь opencode/docker не проверен вживую (в песочнице нет Docker) — при первом прогоне сверить формат вывода `opencode run` без `--format json`.
- [ ] Разработать генератор промтов — спроектировано, см. `ai-platform/docs/promptgen-spec.md` (промт: `ai-platform/docs/promptgen-prompt.md`). Универсальный «улучшатель промтов»: one-shot `streamClaude`, tool-capable grounding по репозиторию, правая панель (клон GeminiPanel, Gemini переезжает влево), гибкие настройки генерации, кнопки «Скопировать» / «Передать менеджеру» (goal → loop).
- [ ] В проектах кнопку открытия клауда заменить на открытие проекта
- [ ] Добавить гибкий буфер обмена
- [ ] Выбор модели убрать из чоисов, делать вкладками там где claude, shell, git. Кнопка START REUME WITH TASK к "новая задача"
- [ ] Время в хедере сделать в формате AM PM
- [ ] Починить создание мра
- [ ] Добавить уведомление звуков и в title страницы о завершении действия или ожидания ответа
- [x] sk-ws-H.XHIHEL.UCgD.MEYCIQCK-QzMni3OF_RRjmOzCyd2FKdk8Hvbd212KZCbqqMHuwIhAIL1Nij2rJBEVeebdVEftWFheZqs13txl4zQKHMzHrep апи, https://dashscope-intl.aliyuncs.com/apps/anthropic ссылка, надо сюда прикрутить еще и квина

## Примечания

- opencode сделан в минимальном объёме: он ставится в контейнер, в тулбаре проекта
  появляется выбор агента (Claude Code / opencode). Модель выбирается внутри самого
  opencode (`/models`), ключи провайдеров — через `opencode auth login` на вкладке Shell.
